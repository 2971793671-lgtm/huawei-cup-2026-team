# 本地原始数据目录

本目录只存放比赛题目提供或队伍下载的原始数据，不进入公开 Git 仓库。当前目录中的附件包体积较大，且可能包含不适合公开分发的内容。

跨设备协作时，请把数据来源、版本、文件大小、SHA256、许可边界和处理关系登记到 [../manifests/](../manifests/)，处理脚本从 manifest 读取路径，不把原始文件复制进 Git。

如果需要让队友复现，请通过受控网盘或对象存储传输，并在 manifest 中记录传输后的校验值。
