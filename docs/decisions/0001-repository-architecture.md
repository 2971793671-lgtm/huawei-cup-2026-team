# 决策 0001：采用 Git + LaTeX + 数据清单的统一架构

日期：2026-09-21

## 决定

代码、配置、论文源文件和协作文档进入私有 Git；大型数据和模型通过数据仓库或共享存储管理；论文采用 LaTeX；实验结果以 run_id、配置、数据哈希和 Git commit 关联。

## 原因

三人跨设备修改需要可审计的版本历史。LaTeX 纯文本便于章节并行修改，数据清单和实验索引可以避免把大文件直接塞进 Git。

## 代价

需要统一安装 XeLaTeX，并要求队员遵守分支、命名和实验记录规则。
